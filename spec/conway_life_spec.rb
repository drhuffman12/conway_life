require "spec_helper"

describe ConwayLife do
  it "has a version number" do
    expect(ConwayLife::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
